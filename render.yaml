# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: sslpinning-api
    runtime: docker
    plan: free
    region: oregon
    # Code is at repo root on the demo branch. Leave Root Directory blank in the Render dashboard.
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /api/health
